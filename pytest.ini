[pytest]
# Pytest configuration
testpaths = test/*
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    anyio: mark tests as async (async def test functions)
addopts = 
    -v
    --strict-markers
    --tb=short
    --disable-warnings

pythonpath = .

