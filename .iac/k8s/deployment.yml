apiVersion: apps/v1
kind: Deployment
metadata:
  name: cezzis-com-cocktails-aisearch
  namespace: cezzis
  labels:
    app: cezzis-com-cocktails-aisearch
    app.kubernetes.io/part-of: cezzis
    app.kubernetes.io/managed-by: argocd
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: cezzis-com-cocktails-aisearch
  template:
    metadata:
      labels:
        app: cezzis-com-cocktails-aisearch
        app.kubernetes.io/part-of: cezzis
        app.kubernetes.io/managed-by: argocd
      annotations:
        dapr.io/enabled: "false"
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: cezzis-com-cocktails-aisearch
          image: acrveceusgloshared001.azurecr.io/cezziscomcocktailsaisearch
          ports:
            - containerPort: 8010
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: cezzis-com-cocktails-aisearch-config
            - secretRef:
                name: cezzis-com-cocktails-aisearch-secrets
            - secretRef:
                name: cezzis-com-cocktails-aisearch-shared-secrets
          startupProbe:
            httpGet:
              path: v1/readiness
              port: 8010
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 30
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /v1/readiness
              port: 8010
            periodSeconds: 15
            timeoutSeconds: 30
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /v1/liveness
              port: 8010
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
